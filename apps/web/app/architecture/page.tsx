import {GitBranch, Server, Shield, Database, Bot, Gauge} from 'lucide-react';
const nodes=[['Web','Next.js documentation, account and community UI',Server],['Custom API','Fastify REST, OpenAPI, RBAC and application services',GitBranch],['Auth','Sessions, TOTP, recovery codes and linked identities',Shield],['PostgreSQL','Users, community, roles, audit and QA state',Database],['Bots','Telegram and Discord security clients',Bot],['Worker','GitHub sync, static QA and background jobs',Gauge]];
export default function Architecture(){return <div className="container py-12"><div className="text-xs uppercase tracking-[.18em] text-cyan-300">Architecture</div><h1 className="mt-2 text-4xl md:text-5xl font-semibold tracking-tight">One platform, shared services.</h1><p className="mt-4 max-w-3xl text-lg leading-8 muted">Web, API, identity, bots and workers are separated by responsibility. The deployment target uses native services; Docker is not required.</p><div className="mt-10 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{nodes.map(([name,desc,I])=>{const Icon=I as any;return <div key={name as string} className="surface rounded-2xl p-6"><div className="grid h-10 w-10 place-items-center rounded-xl accent text-[#031016]"><Icon size={18}/></div><h2 className="mt-4 text-lg font-semibold">{name as string}</h2><p className="mt-2 text-sm leading-6 muted">{desc as string}</p></div>})}</div><div className="mt-8 surface rounded-2xl p-6"><pre className="overflow-auto text-sm leading-7 text-slate-200"><code>{`Web\n  ↓\nCustom API\n  ├── Auth / RBAC / MFA\n  ├── Community\n  ├── GitHub / QA\n  └── Notifications\n       ↓\n    PostgreSQL\n       ↑\nWorkers / Telegram / Discord`}</code></pre></div></div>}
