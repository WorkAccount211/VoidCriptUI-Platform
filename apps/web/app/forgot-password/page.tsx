'use client';import {useState} from 'react';import {api} from '@/lib/api';import Link from 'next/link';
export default function Forgot(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');async function submit(){try{const r=await api('/api/v1/auth/forgot-password',{method:'POST',body:JSON.stringify({email})});setMsg('If the account exists, a secure recovery message has been sent.')}catch(e:any){setMsg(e.message)}}return <main className="container flex min-h-[calc(100vh-64px)] items-center justify-center py-12"><div className="surface w-full max-w-md rounded-3xl p-7"><h1 className="text-3xl font-semibold">Reset password</h1><p className="mt-2 text-sm muted">Enter your verified account email. The production deployment should deliver a single-use reset link through the configured mail provider.</p><input className="mt-6 w-full rounded-xl border border-white/10 bg-white/[.03] px-3 py-3" placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)}/>{msg&&<div className="mt-4 rounded-xl bg-white/[.03] p-3 text-sm">{msg}</div>}<button onClick={submit} className="accent mt-5 w-full rounded-xl px-4 py-3 font-medium text-[#031016]">Request recovery</button><Link href="/sign-in" className="mt-4 block text-center text-sm text-cyan-200">Back to sign in</Link></div></main>}
