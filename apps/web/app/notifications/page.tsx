'use client';import {useEffect,useState} from 'react';import {api} from '@/lib/api';
export default function Notifications(){const [n,setN]=useState<any[]>([]);useEffect(()=>{api('/api/v1/notifications').then(x=>setN(x.data||[])).catch(()=>{})},[]);return <main className="container py-12"><h1 className="text-4xl font-semibold">Notifications</h1><div className="mt-8 surface overflow-hidden rounded-2xl">{n.length?n.map(x=><div key={x.id} className="border-b border-white/5 p-5"><div className="text-sm font-medium">{x.title}</div><div className="mt-1 text-sm muted">{x.body}</div><div className="mt-2 text-xs muted">{new Date(x.createdAt).toLocaleString()}</div></div>):<div className="p-7 text-sm muted">No notifications yet.</div>}</div></main>}
