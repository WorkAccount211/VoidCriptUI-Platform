const steps=[['1','Load the library','Start with the canonical loader from the current repository release.'],['2','Create a Window','Initialize the root UI surface using the verified API.'],['3','Add Tab and Section','Organize controls into a readable information hierarchy.'],['4','Add controls','Use the element pages to discover exact constructors and options.'],['5','Persist state','Use the documented flag/config APIs after verifying them against your release.']];
export default function GettingStarted(){return <><main className="container py-12"><div className="max-w-3xl"><div className="text-xs uppercase tracking-[.18em] text-cyan-300">Getting Started</div><h1 className="mt-2 text-4xl font-semibold">Your first VoidCriptUI interface</h1><p className="mt-4 leading-7 muted">The final examples on this page should be regenerated from the synchronized repository so they remain version-accurate.</p></div><div className="mt-10 space-y-3">{steps.map(([n,t,d])=><div key={n} className="surface rounded-2xl p-5"><div className="flex gap-4"><div className="grid h-8 w-8 shrink-0 place-items-center rounded-lg accent text-xs font-semibold text-[#031016]">{n}</div><div><h2 className="font-medium">{t}</h2><p className="mt-1 text-sm muted">{d}</p></div></div></div>)}</div></main></>}
